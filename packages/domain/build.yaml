# To add configuration for dart_mappable
global_options:
  dart_mappable_builder:
    options:
      ignoreNull: true
      caseStyle: camelCase
      renameMethods:
        fromJson: fromJsonString
        toJson: toJsonString
        fromMap: fromJson
        toMap: toJson